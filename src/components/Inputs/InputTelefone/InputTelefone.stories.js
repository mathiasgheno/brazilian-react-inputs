import { InputTelefone } from './InputTelefone';

export default {
  title: 'InputTelefone',
  component: InputTelefone,
}

const Template = (props) => <InputTelefone {...props} />

export const Normal = Template.bind({});
